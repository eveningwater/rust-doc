fn main(){
    println!("hello,world!");
    // fn is_straight(num_vec: Vec<i32>) -> bool {
    //     let mut sets = std::collections::HashSet::new();
    //     for x in num_vec {
    //         if x != 0 && !sets.insert(x) { return false; }
    //     }
    //     let min = *sets.iter().min().unwrap();
    //     let max = sets.into_iter().max().unwrap();
    //     return max - min < 5;
    // }
    // let res = is_straight(vec![1,2,3,4,5]);
    // println!("res is {}",res);

}
